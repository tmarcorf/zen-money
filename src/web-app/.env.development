# Development mode: Vite proxies /api to the backend, so keep this empty
# so the browser sends requests to the same origin (no CORS).
VITE_API_URL=

# Override the proxy target if your backend is not on http://localhost:5160
# VITE_API_TARGET=http://localhost:5160
